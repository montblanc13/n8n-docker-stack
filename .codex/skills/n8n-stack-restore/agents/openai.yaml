interface:
  display_name: "N8N Stack Restore"
  short_description: "Restore the n8n Docker stack data volumes"
  default_prompt: "Use $n8n-stack-restore to restore the data volumes for this repository's n8n Docker stack."
